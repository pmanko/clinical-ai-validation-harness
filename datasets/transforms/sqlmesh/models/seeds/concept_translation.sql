MODEL (
  name refapp_28_demo.seeds.concept_translation,
  kind SEED (
    path '../../seeds/concept_translation.csv'
  ),
  columns (
    source_concept_id INT,
    source_uuid VARCHAR(38),
    target_concept_id INT,
    target_uuid VARCHAR(38),
    equivalence VARCHAR(32),
    policy_bucket VARCHAR(32),
    source_record_examples VARCHAR(1024)
  ),
  description 'Bridge-rule materialization: one row per distinct legacy concept_id, with target_uuid in the CIEL RPAD(N, 36, A) pattern. Generated by harness.conceptmap.seed_emit.',
  tags (policy_bucket:remap),
  grain (source_concept_id)
);
