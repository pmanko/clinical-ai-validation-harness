MODEL (
  name refapp_28_demo.mod__htmlformentry_html_form,
  kind VIEW,
  description 'Carry-forward orphan: legacy module-owned table preserved verbatim in the materialized output. legacy-only table not in 2.8 RefApp baseline; default carry-forward',
  tags (policy_bucket:orphan_carry_forward),
  grain (id)
);

-- Auto-generated by harness/transform/gen_modules.py from
-- seeds/module_table_policy.csv. Do NOT edit by hand — re-run the
-- generator instead.

SELECT
  src.id,
  src.form_id,
  src.name,
  src.xml_data,
  src.creator,
  @shift_date(src.date_created) AS date_created,
  src.changed_by,
  @shift_date(src.date_changed) AS date_changed,
  src.retired,
  src.uuid,
  src.description,
  src.retired_by,
  @shift_date(src.date_retired) AS date_retired,
  src.retire_reason
FROM legacy_27_raw.htmlformentry_html_form src;
