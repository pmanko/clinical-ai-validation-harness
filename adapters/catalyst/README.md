# Catalyst adapter placeholder

Planned future work:

- capture Gateway -> Agent -> MCP -> SQL preview -> execution traces,
- implement NL-to-SQL consistency checks and validation labels,
- align emitted metadata with shared run/event schema.
