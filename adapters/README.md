# Adapters

Project adapter contracts for invoking real validation paths.

- `chartsearchai/`: Maven and API wrapper plans.
- `querystore/`: CQRS retrieval wrapper plans.
- `openmrs_chatbot/`: placeholder for future conversation/trace adapter.

Catalyst uses its public Gateway routes directly from `harness/catalyst/`; it
does not have a second adapter contract here.
